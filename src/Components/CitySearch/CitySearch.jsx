import React from "react";
import styled from "styled-components";

const CitySearchStyle = styled.div``;

export const CitySearch = () => {
  return (
    <CitySearchStyle>
      <div className="city-dialog-box-container"></div>
    </CitySearchStyle>
  );
};
